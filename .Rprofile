# .Rprofile
#
# by Andrew DeOrio
# 2012-12-11

# general options
options(papersize="letter")
options(editor="emacs")
options(pager="less")
options(tab.width = 2) 
## options(width = 80)
options(Hverbose=FALSE)

# load Hmisc package w/o messages
# suppressMessages(require(Hmisc))

# another way to add a package to the packages loaded by the system
## pkgs = getOption("defaultPackages")  # Get system list of packages to load
## pkgs = c(pkgs, "Hmisc")              # Append "Hmisc" to list
## options(defaultPackages = pkgs)      # Update configuration option
## rm(pkgs)                             # remove tmp variable


## .First <- function(){
## }

## .Last <- function(){ 
## }
