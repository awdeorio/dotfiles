# need this option so that .ssh/known_hosts has plain text hostnames.  Bash
# completion then reads this file to auto-complete hostnames
HashKnownHosts no

# SSH multiplexing
# http://www.linuxjournal.com/content/speed-multiple-ssh-connections-same-server
Host *
  ControlMaster auto
  ControlPath ~/.ssh/master-%r@%h:%p

# Example: jump host
# Ref https://www.cyberciti.biz/faq/linux-unix-ssh-proxycommand-passing-through-one-host-gateway-server/
# Ref https://wiki.gentoo.org/wiki/SSH_jump_host
# Host *.eecs
#   ProxyCommand ssh -q trenton.eecs.umich.edu nc `echo "%h" | tr _ -` %p

# Example: alias for an IP
# Host alice
#   Hostname 10.8.0.30

# Include other SSH config files
Include config-*
